$(document).ready(function() {
	$('ul.ibm_cci--ls--toggle--ul li').on('click', 'a', function(event) {
		// event.preventDefault();
		/* Act on the event */
		var $liElem = $(this).data('id');
		console.log($liElem);
	});
});